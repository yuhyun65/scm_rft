openapi: 3.0.3
info:
  title: "<service> API"
  version: "0.1.0"
servers:
  - url: "http://localhost:8080"
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: UP
